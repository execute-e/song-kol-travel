{
  "name": "client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "vite": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sass-watch": "sass --watch .",
    "dev": "concurrently \"npm run sass-watch\" \"npm run vite\""
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "sass": "^1.90.0",
    "vite": "^7.1.0"
  }
}
